apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev
resources:
  - ../../base
  - fluent-bit-config.yaml
patches:
  - path: patch-dev.yaml
    target:
      kind: Cluster
      name: my-database
secretGenerator:
  # MinIO credentials
  - name: backup-creds
    literals:
      - ACCESS_KEY_ID=minioadmin
      - SECRET_ACCESS_KEY=minioadmin
  # PostgreSQL connection string
  - name: my-database-app
    literals:
      - password=PiyJAYujdJ3a9oUYRoU3CD4FxsRcVHIDb2wLTpBfL0g4oKH6opgCPg9tCdPOLx6k
    options:
      disableNameSuffixHash: true  
configMapGenerator:
  - name: my-database-configmap
    files:
      - schema.sql
    options:
      disableNameSuffixHash: true      
